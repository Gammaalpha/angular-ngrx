{
  "name": "ibm-frontend-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "(cd client/ && npm install);",
    "server-install": "(cd server/ && npm install);",
    "postinstall": "npm run server-install && npm run client-install",
    "start": "concurrently \"(cd client/ && npm start)\" \"(cd server/ && npm run start)\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
